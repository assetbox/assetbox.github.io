---
sidebar_position: 3
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Static Build

Deploying static files in places such as s3 allows designers and developers to share the asset that exists in the project. It's like an in-house icon viewer.

Create a static file in the `assetbox.config.cjs` file based on the `category`.
Build output folder is `assetbox-dist`

In this case, the administrative portion is lost and replaced with read-only.
Can be used as an icon viewer

[Static Build Demo](https://assetbox.github.io/demo)  
[Demo Repository](https://github.com/assetbox/demo)

## Setup

```js twoslash title='assetbox.config.cjs'
/**
 * @type {import('@assetbox/cli').AssetBoxScheme}
 */
const config = {
  ...
  staticBuild: {
    // This field is an optional field. Default value is `assetbox-dist`
    outDir: "assetbox-dist", // Build Output Folder
  },
};

module.exports = config;
```

## Usage

<Tabs>
  <TabItem value="pnpm" label="pnpm" default>

```sh
$ pnpm assetbox static-build
```

  </TabItem>
  <TabItem value="yarn" label="yarn">

```sh
$ yarn assetbox static-build
```

  </TabItem>
  <TabItem value="npm" label="npm">

```sh
$ npm run assetbox static-build
```

  </TabItem>
</Tabs>
